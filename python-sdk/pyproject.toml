[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "claw-sdk"
version = "0.1.0"
description = "Python client stub for the Claw gateway"
requires-python = ">=3.9"
