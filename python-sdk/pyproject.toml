[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "openclaw-sdk"
version = "0.1.0"
description = "Python client stub for the OpenClaw gateway"
requires-python = ">=3.9"
