Run the FindInfo workflow.

topic: $TOPIC

Steps:
1. Initialize variable `result` with Call agent_Researcher
2. Return the result

Expected output format (JSON):
{"url": "string", "snippet": "string", "confidence_score": "number"}
